name               = "otp-verify-proxy"
main               = "src/index.js"
compatibility_date = "2024-09-23"

# After running `wrangler kv namespace create OTP_ATTEMPTS`, paste the returned
# id below and remove the preview_id line if you don't need a staging namespace.
[[kv_namespaces]]
binding    = "OTP_ATTEMPTS"
id         = "4f089e80e62a4ddc8192f7b78b7d7308"

# FIREBASE_VERIFY_OTP_URL is stored as a secret (never in this file).
# Set it with: wrangler secret put FIREBASE_VERIFY_OTP_URL
